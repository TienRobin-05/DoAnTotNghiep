@* Khu vực giao diện của view traLoi.
   View này nhận dữ liệu từ Controller/Model và hiển thị cho người dùng theo đúng chức năng của màn hình. *@
@model cauHoiTuVanModels
@{
    ViewData["Title"] = "Tráº£ lá»i tÆ° váº¥n";
}

<h2>Tráº£ lá»i cÃ¢u há»i</h2>
<div class="mb-3">
    <strong>NgÆ°á»i gá»­i:</strong> @Model.tenNguoiGui
</div>
<div class="border p-3 mb-3">@Model.cauHoi</div>
@* Khu vực form cho phép người dùng nhập dữ liệu và gửi request về Controller để xử lý. *@
<form asp-action="traLoi" method="post" class="col-md-8">
    @Html.AntiForgeryToken()
    <input type="hidden" name="maCauHoi" value="@Model.maCauHoi" />
    <div class="mb-3">
        <label class="form-label">CÃ¢u tráº£ lá»i</label>
        <textarea name="cauTraLoi" rows="5" class="form-control">@Model.cauTraLoi</textarea>
    </div>
    <button type="submit" class="btn btn-primary">LÆ°u tráº£ lá»i</button>
    <a asp-action="index" class="btn btn-link">Quay láº¡i</a>
</form>
