@* Khu vực giao diện của view Details.
   View này nhận dữ liệu từ Controller/Model và hiển thị cho người dùng theo đúng chức năng của màn hình. *@
@model CauHoiTuVan
@{
    ViewData["Title"] = "Chi tiáº¿t cÃ¢u há»i tÆ° váº¥n";
}

<h2>Chi tiáº¿t cÃ¢u há»i tÆ° váº¥n</h2>

<div class="card">
    <div class="card-body">
        <p><strong>NgÆ°á»i gá»­i:</strong> @Model.TenNguoiGui</p>
        <p><strong>NgÃ y gá»­i:</strong> @Model.NgayGui.ToString("dd/MM/yyyy HH:mm")</p>
        <p><strong>Tráº¡ng thÃ¡i:</strong> @Model.TrangThai</p>
        <hr />
        <p><strong>CÃ¢u há»i:</strong></p>
        <p>@Model.CauHoi</p>
        <hr />
        <p><strong>CÃ¢u tráº£ lá»i:</strong></p>
        @* Khối điều kiện bên dưới chỉ hiển thị nội dung khi dữ liệu hoặc trạng thái tương ứng tồn tại. *@
        @if (string.IsNullOrWhiteSpace(Model.CauTraLoi))
        {
            <p class="text-muted">ChÆ°a cÃ³ cÃ¢u tráº£ lá»i.</p>
        }
        else
        {
            <p>@Model.CauTraLoi</p>
            <p><strong>NgÆ°á»i tráº£ lá»i:</strong> @Model.TenNguoiTraLoi</p>
            <p><strong>NgÃ y tráº£ lá»i:</strong> @Model.NgayTraLoi?.ToString("dd/MM/yyyy HH:mm")</p>
        }
    </div>
</div>

<a asp-action="Index" class="btn btn-secondary mt-3">Quay láº¡i</a>
