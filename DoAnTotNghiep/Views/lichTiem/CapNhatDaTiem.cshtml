@* Khu vực giao diện của view CapNhatDaTiem.
   View này nhận dữ liệu từ Controller/Model và hiển thị cho người dùng theo đúng chức năng của màn hình. *@
@model LichTiem
@{
    ViewData["Title"] = "Cáº­p nháº­t Ä‘Ã£ tiÃªm";
    var ngayTiemThucTe = ViewBag.NgayTiemThucTe as string ?? DateTime.Today.ToString("yyyy-MM-dd");
    var ghiChu = ViewBag.GhiChu as string ?? string.Empty;
}

<h2>Cáº­p nháº­t tráº¡ng thÃ¡i Ä‘Ã£ tiÃªm</h2>

@* Khối điều kiện bên dưới chỉ hiển thị nội dung khi dữ liệu hoặc trạng thái tương ứng tồn tại. *@
@if (ViewBag.ThongBao != null)
{
    @* Khu vực thông báo phản hồi kết quả xử lý, ví dụ thành công, lỗi nhập liệu hoặc lỗi nghiệp vụ. *@
    <div class="alert alert-danger">@ViewBag.ThongBao</div>
}

<div class="card mb-3">
    <div class="card-body">
        <p><strong>Há»“ sÆ¡:</strong> @Model.HoTenHoSo</p>
        <p><strong>Vaccine:</strong> @Model.TenVaccine</p>
        <p><strong>MÅ©i tiÃªm:</strong> @Model.TenMui - MÅ©i @Model.SoMui</p>
        <p><strong>NgÃ y tiÃªm dá»± kiáº¿n:</strong> @Model.NgayTiemDuKien.ToString("dd/MM/yyyy")</p>
        <p><strong>Tráº¡ng thÃ¡i hiá»‡n táº¡i:</strong> @Model.TrangThai</p>
    </div>
</div>

@* Khu vực form cho phép người dùng nhập dữ liệu và gửi request về Controller để xử lý. *@
<form asp-action="CapNhatDaTiem" method="post" class="col-md-6">
    @Html.AntiForgeryToken()
    <input type="hidden" name="maLichTiem" value="@Model.MaLichTiem" />

    <div class="mb-3">
        <label class="form-label">NgÃ y tiÃªm thá»±c táº¿</label>
        <input name="ngayTiemThucTe" type="date" class="form-control" value="@ngayTiemThucTe" required />
    </div>

    <div class="mb-3">
        <label class="form-label">Ghi chÃº</label>
        <textarea name="ghiChu" class="form-control" rows="4">@ghiChu</textarea>
    </div>

    <button type="submit" class="btn btn-primary">LÆ°u cáº­p nháº­t</button>
    <a asp-action="Index" asp-route-maHoSo="@Model.MaHoSo" class="btn btn-secondary">Quay láº¡i</a>
</form>
