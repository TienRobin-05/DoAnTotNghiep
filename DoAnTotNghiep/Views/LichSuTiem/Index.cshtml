@* Khu vực giao diện của view Index.
   View này nhận dữ liệu từ Controller/Model và hiển thị cho người dùng theo đúng chức năng của màn hình. *@
@model List<LichSuTiem>
@{
    ViewData["Title"] = "Lịch sử tiêm";
}

<div class="list-page-head">
    <div>
        <h2>Lịch sử tiêm của @ViewBag.HoTenHoSo</h2>
        <p>@Model.Count mũi tiêm đã được ghi nhận</p>
    </div>
    <a asp-action="ChonHoSo" class="btn btn-link">Chọn hồ sơ khác</a>
</div>

@* Khối điều kiện bên dưới chỉ hiển thị nội dung khi dữ liệu hoặc trạng thái tương ứng tồn tại. *@
@if (Model.Count == 0)
{
    @* Khu vực thông báo phản hồi kết quả xử lý, ví dụ thành công, lỗi nhập liệu hoặc lỗi nghiệp vụ. *@
    <div class="alert alert-info">Chưa có lịch sử tiêm</div>
}
else
{
    <section class="list-card">
    @* Khu vực bảng dùng để hiển thị danh sách dữ liệu theo từng cột, giúp người dùng dễ theo dõi và thao tác. *@
    <table class="table list-table">
        <thead>
            <tr>
                <th>Vaccine</th>
                <th>Mũi tiêm</th>
                <th>Ngày dự kiến</th>
                <th>Ngày thực tế</th>
                <th>Ghi chú</th>
                <th>Ngày cập nhật</th>
            </tr>
        </thead>
        <tbody>
        @* Vòng lặp bên dưới duyệt danh sách dữ liệu do Controller truyền sang để hiển thị từng dòng/từng mục trên giao diện. *@
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <div class="item-title">@item.TenVaccine</div>
                    <div class="item-meta">@item.HoTenHoSo</div>
                </td>
                <td>
                    <span class="soft-badge">Mũi @item.SoMui</span>
                    <div class="item-meta">@item.TenMui</div>
                </td>
                <td>@item.NgayTiemDuKien.ToString("dd/MM/yyyy")</td>
                <td><span class="status-badge status-ok">@item.NgayTiemThucTe.ToString("dd/MM/yyyy")</span></td>
                <td class="text-truncate-cell">@item.GhiChu</td>
                <td>@item.NgayCapNhat.ToString("dd/MM/yyyy HH:mm")</td>
            </tr>
        }
        </tbody>
    </table>
    </section>
}
